# Copy to .env.local and fill in. Never commit real secrets.

# --- Auth.js v5 / Google OAuth (U3) ---
# Generate with: npx auth secret
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# Comma-separated allowlist of the two adult Google emails (U3).
# NOTE: CONTEXT seeds @gmail.com addresses — set the real two emails here.
ALLOWED_EMAILS=hendrik.buender@gmail.com,jennifer.buender@gmail.com

# --- Database: libSQL / SQLite (U2) ---
# Local file to start (created on first migrate). Swap to a hosted Turso URL
# later with no code change: DATABASE_URL=libsql://<db>.turso.io
DATABASE_URL=file:./famealy.db
# Only needed for a hosted Turso DB; leave blank for the local file.
DATABASE_AUTH_TOKEN=

# --- OpenAI (U7 vision import, U9 starter set, U14 arrangement) ---
OPENAI_API_KEY=
